---
- name: Check SELinux status
  command: getenforce
  register: selinux_status
  changed_when: false
  failed_when: false

- name: Disable SELinux permanently
  selinux:
    state: disabled
  when: selinux_status.stdout != "Disabled"

- name: Disable SELinux for current session
  shell: setenforce 0
  when: selinux_status.stdout != "Disabled"

- name: Install docker-ce
  package:
    name:
      - docker-ce
      - docker-compose-plugin
    state: present

- name: Ensure Docker service is running
  service:
    name: docker
    state: started
    enabled: yes

- name: Load iptables kernel modules
  shell: |
    modprobe ip_tables || true
    modprobe iptable_filter || true
    modprobe iptable_nat || true
    modprobe xt_conntrack || true
  changed_when: false

- name: Ensure iptables modules load on boot
  copy:
    content: |
      ip_tables
      iptable_filter
      iptable_nat
      xt_conntrack
    dest: /etc/modules-load.d/iptables.conf
    mode: '0644'

- name: Start and enable firewalld
  systemd:
    name: firewalld
    state: started
    enabled: yes

- name: Open firewall ports for media stack services
  firewalld:
    port: "{{ item }}"
    permanent: yes
    state: enabled
    immediate: yes
  loop:
    - 80/tcp        # HAProxy HTTP
    - 443/tcp       # HAProxy HTTPS
    - 139/tcp       # Samba
    - 445/tcp       # Samba
    - 5055/tcp      # Overseerr
    - 6767/tcp      # Bazarr
    - 7878/tcp      # Radarr
    - 8080/tcp      # qBittorrent
    - 8081/tcp      # Dashboard
    - 8404/tcp      # HAProxy Stats
    - 8989/tcp      # Sonarr
    - 9000/tcp      # Portainer
    - 9117/tcp      # Jackett
    - 9696/tcp      # Prowlarr
    - 32400/tcp     # Plex

- name: Ensure base appdata directory exists
  file:
    path: "{{ appdata_dir }}"
    state: directory
    mode: '0755'

- name: Ensure media directory exists
  file:
    path: "{{ media_dir }}"
    state: directory
    owner: 911
    group: 100
    mode: '0775'

- name: Fix media directory permissions recursively
  file:
    path: "{{ media_dir }}"
    owner: 911
    group: 100
    mode: '0775'
    recurse: yes

- name: Ensure downloads directory exists
  file:
    path: "{{ downloads_dir }}"
    state: directory
    owner: 911
    group: 100
    mode: '0775'

- name: Fix downloads directory permissions recursively
  file:
    path: "{{ downloads_dir }}"
    owner: 911
    group: 100
    mode: '0775'
    recurse: yes

- name: Ensure transcode directory exists
  file:
    path: "{{ transcode_dir }}"
    state: directory
    owner: 911
    group: 100
    mode: '0775'

- name: Create portainer directory
  file:
    path: "{{ appdata_dir }}/portainer"
    state: directory
    mode: '0755'

- name: Create dashboard directory
  file:
    path: "{{ appdata_dir }}/dashboard"
    state: directory
    mode: '0755'

- name: Deploy dashboard HTML
  template:
    src: index.html.j2
    dest: "{{ appdata_dir }}/dashboard/index.html"
    mode: '0644'

- name: Create haproxy config directory
  file:
    path: "{{ appdata_dir }}/haproxy"
    state: directory
    mode: '0755'

- name: Generate self-signed SSL certificate for HAProxy
  shell: |
    if [ ! -f {{ appdata_dir }}/haproxy/cert.pem ]; then
      openssl req -x509 -nodes -days 3650 -newkey rsa:2048 \
        -keyout {{ appdata_dir }}/haproxy/cert.key \
        -out {{ appdata_dir }}/haproxy/cert.crt \
        -subj "/C=US/ST=State/L=City/O=Media Stack/CN={{ ansible_host }}"
      cat {{ appdata_dir }}/haproxy/cert.crt {{ appdata_dir }}/haproxy/cert.key > {{ appdata_dir }}/haproxy/cert.pem
      chmod 644 {{ appdata_dir }}/haproxy/cert.pem
    fi
  args:
    creates: "{{ appdata_dir }}/haproxy/cert.pem"

- name: Ensure certificate permissions are correct for Docker
  file:
    path: "{{ appdata_dir }}/haproxy/cert.pem"
    owner: root
    group: root
    mode: '0644'

- name: Deploy haproxy configuration
  template:
    src: haproxy.cfg.j2
    dest: "{{ appdata_dir }}/haproxy/haproxy.cfg"
    mode: '0644'

- name: Create qBittorrent config directory
  file:
    path: "{{ appdata_dir }}/binhex-qbittorrentvpn/qBittorrent/config"
    state: directory
    owner: 911
    group: 100
    mode: '0755'
    recurse: yes

- name: Deploy qBittorrent configuration with file exclusion list
  template:
    src: qBittorrent.conf.j2
    dest: "{{ appdata_dir }}/binhex-qbittorrentvpn/qBittorrent/config/qBittorrent.conf"
    owner: 911
    group: 100
    mode: '0644'

- name: Copy docker-compose.yml 
  template:
    src: docker-compose.yml.j2
    dest: "{{ appdata_dir }}/docker-compose.yml"
    mode: '0644'

- name: Deploy media stack 
  shell: docker compose up -d --force-recreate
  args:
    chdir: "{{ appdata_dir }}"

- name: Add hourly cron job to restart qBittorrent container
  cron:
    name: "Restart qBittorrent container"
    minute: "0"
    hour: "*"
    user: root
    job: "cd {{ appdata_dir }} && docker compose restart qbittorrent >> /var/log/qbittorrent-restart.log 2>&1"
    state: present

- name: Fix SELinux context for Docker volumes after deployment
  shell: |
    # Set appropriate SELinux context for Docker volumes
    chcon -R -t container_file_t {{ appdata_dir }} 2>/dev/null || true
    # Restart HAProxy to ensure it can read the certificate
    cd {{ appdata_dir }} && docker compose restart haproxy
  failed_when: false

# Recyclarr Configuration
- name: Create recyclarr config directory
  file:
    path: "{{ appdata_dir }}/recyclarr"
    state: directory
    owner: 911
    group: 100
    mode: '0755'

- name: Deploy Recyclarr configuration
  template:
    src: recyclarr.yml.j2
    dest: "{{ appdata_dir }}/recyclarr/recyclarr.yml"
    owner: 911
    group: 100
    mode: '0644'

# Malware Monitor Configuration
- name: Create malware-monitor config directory
  file:
    path: "{{ appdata_dir }}/malware-monitor"
    state: directory
    mode: '0755'

- name: Deploy Malware Monitor script
  template:
    src: exe-monitor.sh.j2
    dest: "{{ appdata_dir }}/malware-monitor/malware-monitor.sh"
    mode: '0755'

# Wait for services to be ready before API configuration
- name: Wait for Sonarr to be ready
  uri:
    url: "http://{{ ansible_host }}:8989/api/v3/system/status"
    headers:
      X-Api-Key: "{{ sonarr_api_key }}"
    method: GET
    status_code: 200
  register: sonarr_ready
  until: sonarr_ready.status == 200
  retries: 30
  delay: 10
  when: sonarr_api_key is defined

- name: Wait for Radarr to be ready
  uri:
    url: "http://{{ ansible_host }}:7878/api/v3/system/status"
    headers:
      X-Api-Key: "{{ radarr_api_key }}"
    method: GET
    status_code: 200
  register: radarr_ready
  until: radarr_ready.status == 200
  retries: 30
  delay: 10
  when: radarr_api_key is defined

- name: Wait for Prowlarr to be ready
  uri:
    url: "http://{{ ansible_host }}:9696/api/v1/system/status"
    headers:
      X-Api-Key: "{{ prowlarr_api_key }}"
    method: GET
    status_code: 200
  register: prowlarr_ready
  until: prowlarr_ready.status == 200
  retries: 30
  delay: 10
  when: prowlarr_api_key is defined

# Configure Sonarr via API
- name: Check if Sonarr qBittorrent download client exists
  uri:
    url: "http://{{ ansible_host }}:8989/api/v3/downloadclient"
    headers:
      X-Api-Key: "{{ sonarr_api_key }}"
    method: GET
  register: sonarr_download_clients
  when: sonarr_api_key is defined

- name: Configure Sonarr download client (qBittorrent)
  uri:
    url: "http://{{ ansible_host }}:8989/api/v3/downloadclient"
    method: POST
    headers:
      X-Api-Key: "{{ sonarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      enable: true
      protocol: "torrent"
      priority: 1
      removeCompletedDownloads: true
      removeFailedDownloads: true
      name: "qBittorrent"
      implementation: "QBittorrent"
      configContract: "QBittorrentSettings"
      fields:
        - name: "host"
          value: "{{ ansible_host }}"
        - name: "port"
          value: 8080
        - name: "username"
          value: "admin"
        - name: "password"
          value: "adminadmin"
        - name: "category"
          value: "sonarr"
        - name: "recentTvPriority"
          value: 0
        - name: "olderTvPriority"
          value: 0
        - name: "initialState"
          value: 0
      tags: []
    status_code: [201, 400]
  when: 
    - sonarr_api_key is defined
    - sonarr_download_clients.json | selectattr('name', 'equalto', 'qBittorrent') | list | length == 0
  register: sonarr_qbit_result
  failed_when: 
    - sonarr_qbit_result.status == 400
    - "'already exists' not in (sonarr_qbit_result.json.message | default(''))"

# Configure Radarr via API
- name: Check if Radarr qBittorrent download client exists
  uri:
    url: "http://{{ ansible_host }}:7878/api/v3/downloadclient"
    headers:
      X-Api-Key: "{{ radarr_api_key }}"
    method: GET
  register: radarr_download_clients
  when: radarr_api_key is defined

- name: Configure Radarr download client (qBittorrent)
  uri:
    url: "http://{{ ansible_host }}:7878/api/v3/downloadclient"
    method: POST
    headers:
      X-Api-Key: "{{ radarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      enable: true
      protocol: "torrent"
      priority: 1
      removeCompletedDownloads: true
      removeFailedDownloads: true
      name: "qBittorrent"
      implementation: "QBittorrent"
      configContract: "QBittorrentSettings"
      fields:
        - name: "host"
          value: "{{ ansible_host }}"
        - name: "port"
          value: 8080
        - name: "username"
          value: "admin"
        - name: "password"
          value: "adminadmin"
        - name: "category"
          value: "radarr"
        - name: "recentMoviePriority"
          value: 0
        - name: "olderMoviePriority"
          value: 0
        - name: "initialState"
          value: 0
      tags: []
    status_code: [201, 400]
  when: 
    - radarr_api_key is defined
    - radarr_download_clients.json | selectattr('name', 'equalto', 'qBittorrent') | list | length == 0
  register: radarr_qbit_result
  failed_when: 
    - radarr_qbit_result.status == 400
    - "'already exists' not in (radarr_qbit_result.json.message | default(''))"

# Configure Sonarr to prefer releases with more seeders
- name: Get Sonarr indexer config
  uri:
    url: "http://{{ ansible_host }}:8989/api/v3/config/indexer"
    headers:
      X-Api-Key: "{{ sonarr_api_key }}"
    method: GET
  register: sonarr_indexer_config
  when: sonarr_api_key is defined

- name: Configure Sonarr indexer preferences (prefer more seeders)
  uri:
    url: "http://{{ ansible_host }}:8989/api/v3/config/indexer/{{ sonarr_indexer_config.json.id }}"
    method: PUT
    headers:
      X-Api-Key: "{{ sonarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      id: "{{ sonarr_indexer_config.json.id }}"
      minimumAge: 0
      retention: 0
      maximumSize: 0
      rssSyncInterval: 60
      preferIndexerFlags: true
      availabilityDelay: 0
      allowHardcodedSubs: true
      whitelistedHardcodedSubs: ""
    status_code: [200, 202]
  when: 
    - sonarr_api_key is defined
    - sonarr_indexer_config.json is defined

# Configure Radarr to prefer releases with more seeders
- name: Get Radarr indexer config
  uri:
    url: "http://{{ ansible_host }}:7878/api/v3/config/indexer"
    headers:
      X-Api-Key: "{{ radarr_api_key }}"
    method: GET
  register: radarr_indexer_config
  when: radarr_api_key is defined

- name: Configure Radarr indexer preferences (prefer more seeders)
  uri:
    url: "http://{{ ansible_host }}:7878/api/v3/config/indexer/{{ radarr_indexer_config.json.id }}"
    method: PUT
    headers:
      X-Api-Key: "{{ radarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      id: "{{ radarr_indexer_config.json.id }}"
      minimumAge: 0
      retention: 0
      maximumSize: 0
      rssSyncInterval: 60
      preferIndexerFlags: true
      availabilityDelay: 0
      allowHardcodedSubs: true
      whitelistedHardcodedSubs: ""
    status_code: [200, 202]
  when: 
    - radarr_api_key is defined
    - radarr_indexer_config.json is defined

# Configure Failed Download Handling
- name: Get Sonarr download client config
  uri:
    url: "http://{{ ansible_host }}:8989/api/v3/config/downloadclient"
    headers:
      X-Api-Key: "{{ sonarr_api_key }}"
    method: GET
  register: sonarr_downloadclient_config
  when: sonarr_api_key is defined

- name: Enable Sonarr failed download handling (auto-blacklist and retry)
  uri:
    url: "http://{{ ansible_host }}:8989/api/v3/config/downloadclient/{{ sonarr_downloadclient_config.json.id }}"
    method: PUT
    headers:
      X-Api-Key: "{{ sonarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      id: "{{ sonarr_downloadclient_config.json.id }}"
      enableCompletedDownloadHandling: true
      removeCompletedDownloads: true
      removeFailedDownloads: true
      autoRedownloadFailed: true
      autoRedownloadFailedFromInteractiveSearch: true
      checkForFinishedDownloadInterval: 1
    status_code: [200, 202]
  when: 
    - sonarr_api_key is defined
    - sonarr_downloadclient_config.json is defined

- name: Get Radarr download client config
  uri:
    url: "http://{{ ansible_host }}:7878/api/v3/config/downloadclient"
    headers:
      X-Api-Key: "{{ radarr_api_key }}"
    method: GET
  register: radarr_downloadclient_config
  when: radarr_api_key is defined

- name: Enable Radarr failed download handling (auto-blacklist and retry)
  uri:
    url: "http://{{ ansible_host }}:7878/api/v3/config/downloadclient/{{ radarr_downloadclient_config.json.id }}"
    method: PUT
    headers:
      X-Api-Key: "{{ radarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      id: "{{ radarr_downloadclient_config.json.id }}"
      enableCompletedDownloadHandling: true
      removeCompletedDownloads: true
      removeFailedDownloads: true
      autoRedownloadFailed: true
      autoRedownloadFailedFromInteractiveSearch: true
      checkForFinishedDownloadInterval: 1
    status_code: [200, 202]
  when: 
    - radarr_api_key is defined
    - radarr_downloadclient_config.json is defined

# Run Recyclarr sync
- name: Run Recyclarr sync
  shell: |
    docker exec recyclarr recyclarr sync
  when: 
    - sonarr_api_key is defined
    - radarr_api_key is defined
  register: recyclarr_sync
  failed_when: false

- name: Display Recyclarr sync result
  debug:
    msg: "{{ recyclarr_sync.stdout_lines }}"
  when: recyclarr_sync.stdout_lines is defined

# Add daily Recyclarr sync cron job
- name: Add daily cron job to sync Recyclarr
  cron:
    name: "Sync Recyclarr configuration"
    minute: "0"
    hour: "6"
    user: root
    job: "cd {{ appdata_dir }} && docker exec recyclarr recyclarr sync >> /var/log/recyclarr-sync.log 2>&1"
    state: present

# Prowlarr App Sync - Auto-configure Sonarr and Radarr in Prowlarr
- name: Check if Sonarr app exists in Prowlarr
  uri:
    url: "http://{{ ansible_host }}:9696/api/v1/applications"
    headers:
      X-Api-Key: "{{ prowlarr_api_key }}"
    method: GET
  register: prowlarr_apps
  when: prowlarr_api_key is defined

- name: Add Sonarr to Prowlarr applications
  uri:
    url: "http://{{ ansible_host }}:9696/api/v1/applications"
    method: POST
    headers:
      X-Api-Key: "{{ prowlarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      name: "Sonarr"
      syncLevel: "addOnly"
      implementation: "Sonarr"
      configContract: "SonarrSettings"
      fields:
        - name: "baseUrl"
          value: "http://{{ ansible_host }}:8989"
        - name: "apiKey"
          value: "{{ sonarr_api_key }}"
        - name: "syncCategories"
          value: [5000, 5010, 5020, 5030, 5040, 5045, 5050, 5090]
      tags: []
    status_code: [201, 400]
  when: 
    - prowlarr_api_key is defined
    - sonarr_api_key is defined
    - prowlarr_apps.json | selectattr('name', 'equalto', 'Sonarr') | list | length == 0
  register: prowlarr_sonarr_result
  failed_when: 
    - prowlarr_sonarr_result.status == 400
    - "'already exists' not in (prowlarr_sonarr_result.json[0].errorMessage | default(''))"

- name: Add Radarr to Prowlarr applications
  uri:
    url: "http://{{ ansible_host }}:9696/api/v1/applications"
    method: POST
    headers:
      X-Api-Key: "{{ prowlarr_api_key }}"
      Content-Type: "application/json"
    body_format: json
    body:
      name: "Radarr"
      syncLevel: "addOnly"
      implementation: "Radarr"
      configContract: "RadarrSettings"
      fields:
        - name: "baseUrl"
          value: "http://{{ ansible_host }}:7878"
        - name: "apiKey"
          value: "{{ radarr_api_key }}"
        - name: "syncCategories"
          value: [2000, 2010, 2020, 2030, 2040, 2045, 2050, 2060, 2070, 2080, 2090]
      tags: []
    status_code: [201, 400]
  when: 
    - prowlarr_api_key is defined
    - radarr_api_key is defined
    - prowlarr_apps.json | selectattr('name', 'equalto', 'Radarr') | list | length == 0
  register: prowlarr_radarr_result
  failed_when: 
    - prowlarr_radarr_result.status == 400
    - "'already exists' not in (prowlarr_radarr_result.json[0].errorMessage | default(''))"

- name: Display Prowlarr sync status
  debug:
    msg: "Prowlarr apps configured - Indexers will auto-sync to Sonarr and Radarr"
  when: 
    - prowlarr_api_key is defined
    - sonarr_api_key is defined
    - radarr_api_key is defined


